# MD 标签与包装编辑器

在 ElectronWMD 左下角点击 **MD Label Editor**；设备页菜单也有入口，能携带当前选中的曲目。欢迎页无需连接设备即可打开。

## 模板与数据

支持标准标签、全面标签、J-Card（3–5 面板）、封面（1–6 面板）、托盘卡。在左侧内容工具顶部的模板下拉菜单切换（窄窗口先打开“内容工具”），各自的图层、尺寸和折页设置独立保存；专辑、艺术家、曲目、歌词和制作信息由整个工程共享。文字的“内容来源”可改为“独立文字”，解除关联。

“从当前 MD 导入”复制当前已读取的盘片快照；“导入选中曲目”使用设备页的选择。标签中的改名、排序、增删不会写入 MD。全角标题优先，普通标题作为后备；原始设备标题、索引、分组保留在工程中。艺术家只使用明确存在的元数据，不从歌名猜测。

支持 ElectronWMD 当前/旧版 CSV、UTF-8 M3U/M3U8，以及每行一首的文本。文本尾部 `(3:42)` 会识别为时长；M3U `#EXTINF` 的未知时长保留为空。导入可替换或追加曲目。

## 编辑

- 在“编辑面板”中选择主面板、侧标、书脊或内页，然后在图层列表或预览里选择内容。
- 选中图层后可拖动，或输入毫米坐标、宽高、旋转角度。吸附步长 0.5 mm；锁定后禁止拖动和修改。
- 封面艺术、徽标、贴花和背景可导入 PNG/JPEG/WebP/SVG。SVG 先清理再以最高 2400 px 栅格化；其他图片最长边不超过 4096 px，以移除外部引用并让工程完全自包含。
- 自定义图片、文字、形状、纹理和条码支持复制、删除、调整层级、显隐、透明度。图片支持适应、填充裁切、拉伸、缩放与偏移。
- 思源黑体/宋体随应用打包，覆盖常见中文、日文和拉丁文字。粗体/斜体使用合成轮廓；缺字会提示。歌词、制作信息支持标题、粗体、斜体、简单列表和换行。
- J-Card 的书脊方向可切换底部/左侧；底部模式将整个展开稿旋转 90°，不拉伸图案。减少折页不会删除隐藏面板内容；重新增加即可继续编辑。
- 正反面内容分别编辑。内侧初始为空，需要选择面板后添加内容。
- 全面标签按三个独立机械区域裁切，整幅封面在共同坐标系中保持连续；分片预览只用于检查，编辑请返回组合视图。
- 支持撤销/重做，快捷键为 Ctrl/⌘ Z 与 Shift Ctrl/⌘ Z；输入框内保留原生文字编辑快捷键。

## 保存与打印

当前草稿自动保存到本机 IndexedDB，退出编辑器时会等待保存完成。断开设备不影响草稿。**保存工程** 导出包含五种布局和全部图片的 `.mdlabel` ZIP 文件；“打开”可以重新编辑。此格式版本为 1，未知版本或损坏文件会被拒绝。

“尺寸与折页”中可载入当前模板示例，或下载五类示例工程。载入示例替换当前工程，可撤销；静态示例也在 `examples/labels`，另附含封面图片、二维码与双面 J-Card 的整套工程及实际导出的四页 PDF。

SVG 导出以毫米声明真实尺寸，文字全部转换成路径；不依赖接收电脑的字体。支持完整模板、单面板或全面标签的单独裁片。图像依然是内嵌栅格图像，不会伪装成矢量图。

点击“预览”查看当前模板的完整设计，包括主标签、侧标和所有展开面板，保留内部留白及机械间隙；不显示 A4/Letter 纸张、裁切/校准标记或编辑辅助线。默认适合窗口，“− / ＋”每次增减 10 个百分点，范围 10%–500%；100% 按 96 CSS px/英寸换算，放大后可滚动查看。双面模板可切换正面/背面，Escape 关闭。预览缩放不改变打印尺寸；PDF 仍按打印设置中的纸张、份数、裁切线和双面规则排版。

PDF 必须在 ElectronWMD 桌面应用中导出。勾选五种模板中的任意组合，设置份数、A4/Letter 或自定义纸张，自动排版分页。也可导出同样排版的整页 SVG ZIP。

- 默认 A4、10 mm 页边距、裁切标记开启、2 mm 出血，始终为 100% 实际尺寸。出血可调为 0–5 mm，裁切标记可独立关闭。
- 裁切线为实线；折线为外侧虚线标记。辅助网格、编辑手柄和安全线不会导出。
- 标准标签按主标签和侧标分开导出；全面标签按三个异形裁片和侧标分开导出，独立出血区之间至少留 8 mm。全面标签仍从同一连续封面坐标取图，成品轮廓与装配后的机械间隙不变。
- J-Card、封面、托盘卡仅在整张展开稿的外轮廓出血，内部折线不拆开、不重复出血。矩形采用外置裁切角标（距出血区 1 mm、长 2 mm），异形裁片保留真实轮廓细线。
- 背景色和程序背景向外延伸；贴边图片优先使用裁切框外的原图内容，原图不足时复制边缘像素。仅填补出血区，成品图像不放大，文字和条码不延伸。
- 单模板、单面板／裁片 SVG、整页 SVG 和 PDF 共用裁片及标记规则；画布或纸张按实际占用尺寸计算。编辑和“预览”仍仅显示成品设计。
- 双面稿按正面/背面成对输出；一页中单面模板的背面位置留白。按实际打印设置选择长边/短边翻转。
- 含出血和标记的占用尺寸超过纸张时会报错，不会自动缩小；选择自定义尺寸可导出完整展开稿。
- 先用普通纸打印校准页，以尺子测量实际尺寸和正反套准。软件尺寸校验不能替代具体打印机实测。

旧工程首次打开或恢复草稿时：未升级且出血为 0 mm 的工程自动启用 2 mm 出血及裁切标记，并显示提示；已有非零出血值保留。升级标记随工程保存，之后手动改为 0 mm 不会再次覆盖。工程仍采用版本 1 的 `.mdlabel` ZIP，无需手动转换。

默认尺寸依据参考站实际画布及模板区标注，并可手动修正：标准主标签 36×53 mm，侧标 3.8×59 mm；全面标签 70.3×66.8 mm；J-Card 84.5×73 mm；两面板封面 144.8×104.9 mm；托盘卡 106.1×101.6 mm。参考站部分正文有不同尺寸，未混用。

## 开发验证

渲染器：`npm run test:labels`、`npm run test:titles`、`npx tsc --noEmit`。
主项目：`npm run build:main`、`node --test tests/label-pdf.test.cjs`。

实际检查结果与未验证边界见 [验证记录](MD-LABELS-QA.md)。
渲染器源码位于 `src/labels/`；PDF 隔离窗口和 SVG 校验在主项目 `src/label-pdf*.ts`。所有设计共用渲染与排版核心；导入适配器只依赖数据类型和 CSV 解析，不调用设备服务。

参考：https://vhs.texs.org/zh/minidisc-label 及其四种 MiniDisc 包装模式。字体许可见 `public/label-fonts/OFL.txt`。

### 预览与鼠标调整

画布预览高度偏好默认为 500 px，拖动预览下方的分隔条可在 240–1000 px 间调整；双击恢复默认，键盘聚焦后也可用上下方向键调整。高度偏好会在本机记住，短窗口按可用空间压缩；画布同时考虑宽度和高度适配。工具栏的 1.0× 表示适合画布后的相对倍数，可在 0.5×–3.0× 间调整，点击“适合”回到 1.0×。这个倍数不代表打印比例，也不改变毫米尺寸；独立“预览”窗口仍使用“保存与打印”一节所述的百分比缩放。

点击一个未锁定的图层后，拖动四角或四边手柄调整大小，拖动内部移动位置。按住 Shift 拖动角手柄保持比例。文字手柄改变文本框范围（文字会重新换行，字号仍在属性中设置）；图片按所选适应/填充/拉伸方式显示。旋转图层和底部书脊支持相同操作，每次拖动可一次撤销。

预览底色不再显示装饰网格。只有勾选“网格”才显示 5 mm 网格，网格不会进入 SVG/PDF。

### 离线素材库

徽标、贴花、封面艺术和背景面板内可切换“内置素材”和“工程素材”。应用内置从参考站下载的 327 张图片，包括 Hi-Fi Stereo、Lo-Fi Stereo、Dolby Stereo 媒体徽标、经典品牌、音频贴花、镭射贴花和装饰形状。可按分类筛选或搜索（HiFi 与 Hi-Fi 均可搜索），点击加入当前面板。

已选素材内嵌进 `.mdlabel` 工程及导出文件；使用时不访问网站。工程素材可改名、重复使用，也可移除未使用的图片；仍被任一模板或隐藏面板引用的素材不能直接移除。素材改名和移除均可撤销。内置目录的原图、来源链接与 SHA-256 校验值位于 `public/label-assets/catalog.json`，权利说明见同目录 `SOURCES.md`。

### MD Studio 界面与构建（1.5.5）

编辑器采用银灰、白色与青蓝的工作台界面，欢迎页和顶栏共用简化的 MD 卡匣线稿图标；操作图标统一为圆角线稿，七类内容工具保留文字。完整设计规范见 [DESIGN.md](DESIGN.md)，仅约束标签编辑器，不影响作品颜色、字体及打印底色。

- 桌面左侧为 264 px 的模板与七类内容工具；中央为标签预览；右侧为 304 px 的图层列表和所选图层属性。各区独立滚动，选择图层后可以在预览持续可见时修改内容、样式和毫米位置／尺寸。
- 宽度低于 1120 px 时，从“内容工具”打开左侧抽屉；低于 760 px 时，内容工具与图层属性均使用互斥抽屉。Escape 关闭抽屉或对话框并将焦点返回打开位置。
- 顶部常驻“打开”“保存工程”和“排版 / 导出 PDF”。“草稿已保存到本机”指自动保存在当前用户环境中的草稿；需要备份或转移时仍应点击“保存工程”生成 `.mdlabel` 文件。
- “专辑与歌曲”优先显示共享专辑、艺术家等内容；MD、CSV、M3U 导入放在可展开区域。右侧图层属性按“内容与样式”“位置与尺寸”分组，列表下方保留复制、删除和排序入口。
- 模板菜单显示五种固定示例轮廓与默认展开尺寸；缩略图不受当前工程尺寸影响，实际尺寸显示在预览下方。使用方向键和 Enter 选择，Escape 或点击外部关闭。
- SVG 范围选择、SVG 导出和完整设计“预览”位于画布下方；打印设置与设计预览使用独立弹窗。预览底色、标尺和操作手柄不进入输出。

在主项目执行 `npm run pack:labels`，会完整重建渲染器、主进程及 preload，检查版本桥接和标题转换入口后打包 Apple Silicon 应用。产物位于 `build/md-labels/mac-arm64/ElectronWMD.app`。桌面版本为 `0.5.2-1.5.5`，上游渲染器仍为 `1.6.0`。旧 `.mdlabel` 工程无需迁移。

### MD 导入、图层与本机字体

“汉字转换”提供保持原格式（默认）、日文汉字 → 简体中文、日文汉字 → 繁体中文，本机记住选择。全盘和选中曲目均适用，转换盘名、标题、已有艺术家/专辑/分组；保持原始标题备份、顺序和时长。它只做汉字字形转换，不是日文翻译，不向设备写入；CSV/M3U 不受此选项影响。

图层列表顶端为最前方。拖动左侧六点手柄，蓝色插入线指示放置位置；一次拖动可一次撤销。“上移/下移”执行相同范围的排序，仅影响当前模板、面板与正反面。眼睛表示显示，划线眼睛表示隐藏；点击显隐按钮不会切换选中图层。锁定图层不能排序，但可显隐。显隐和排序保存在工程中并用于所有预览与导出。

字体仍提供内置思源黑体/宋体及等宽排版。点击“加载本机字体”，搜索字体名称或样式，再选择具体样式；仅在选择时读取字体字形数据。支持有可用矢量轮廓的 TrueType/OpenType 字体，无法解析时保留当前选择并显示错误。编辑、预览及 SVG/PDF 共用 Fontkit 解析的字形轮廓。

工程仅保存本机字体的 PostScript 名称、字体族和样式，不包含字体文件或安装路径。重开时自动匹配本机字体；缺失时保留引用、标记图层并暂用内置字体显示。受影响的可见内容在导出前必须加载原字体或在字体框中明确选择替代字体。SVG/PDF 已转为路径，不依赖接收电脑安装字体；编辑 `.mdlabel` 仍需对应字体。旧工程无需迁移，旧版应用无法完整还原本机字体设置。
